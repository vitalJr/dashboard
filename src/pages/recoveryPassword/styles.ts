import styled from 'styled-components';

export const Paragraphy = styled.p.withConfig({ displayName: 'Paragraphy' })`
  margin-bottom: 24px;
  font-size: 12px;
`;
export const ContainerForm = styled.div.withConfig({
  displayName: 'ContainerForm',
})`
  margin-bottom: 20px;
`;
